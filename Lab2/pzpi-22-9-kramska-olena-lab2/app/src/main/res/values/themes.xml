<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.WineControl" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@android:color/black</item>
        <item name="editTextStyle">@style/CustomEditText</item>
    </style>

    <style name="Theme.WineControl" parent="Base.Theme.WineControl" />

    <!-- Стиль для EditText -->
    <style name="CustomEditText" parent="android:Widget.EditText">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textColorHint">#AAAAAA</item>
        <item name="android:background">@drawable/edittext_background</item>
    </style>
</resources>
